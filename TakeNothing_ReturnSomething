/*Take Nothing return Something*/ 
#include<stdio.h>
int add(void);
int main()
{
	int s;
	s=add();
	printf("%d",s);
}
int add()
{
  int a,b,c;
  printf("Enter two Numbers:");
  scanf("%d%d",&a,&b);
  c=a+b;
  return(c);
}
