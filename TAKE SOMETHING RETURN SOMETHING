/*TAKE SOMETHING RETURN SOMETHING*/
#include<stdio.h>
int add(int,int);
main()
{
	int a,b,c;
	printf("Enter Two Number:");
	scanf("%d%d",&a,&b);
	c=add(a,b);
    printf("%d",c);	
}
int add(int x,int y)
{
	int z;
	z=x+y;
	return(z);
}
